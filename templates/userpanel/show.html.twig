{% extends 'frontbase.html.twig' %}

{% block title %} Üye bilgileri {% endblock %}

{% block sidebar %}

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {{ include ('home/_userssidebar.html.twig') }}
    {% endif %}

{% endblock %}

{% block body %}

    <div class="container">



        <div class="col-sm-5">
            {{ include('user/messages.html.twig') }}
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ app.user.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ app.user.email }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ app.user.roles|join}}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ app.user.name }}</td>
                </tr>
                <tr>
                    <th>Adres</th>
                    <td>{{ app.user.address }}</td>
                </tr>

                <tr>
                    <th>Şehir</th>
                    <td>{{ app.user.city }}</td>
                </tr>
                <tr>
                    <th>Telefon</th>
                    <td>{{ app.user.number }}</td>
                </tr>

                </tbody>
            </table>
        </div>

    </div>
    </div></div></div>




    </div>

    </div>
    </div></div></div>




{% endblock %}



