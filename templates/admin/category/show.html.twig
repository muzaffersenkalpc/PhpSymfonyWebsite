    {% extends 'adminbase.html.twig' %}

{% block title %}Category{% endblock %}

{% block content %}
    <div id="content">

        <div class="inner" style="min-height:1200px;">
            <div class="row">
                <div class="col-lg-12">
                    <h1 text align="center" >Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Parentid</th>
                <td>{{ category.parentid }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category.title }}</td>
            </tr>
            <tr>
                <th>Keywords</th>
                <td>{{ category.keywords }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ category.status }}</td>
            </tr>
        </tbody>
    </table>

                    <a  class="btn btn-warning"  href="{{ path('admin_category_index') }}"><i class="icon-arrow-left"></i> Listeye geri dön</a>

                    <a class="btn btn-primary",  href="{{ path('admin_category_edit', {'id': category.id}) }}"><i class="icon-pencil icon-white"></i> edit</a>


                </div>
            </div>

            <hr />




        </div>




    </div>
{% endblock %}
