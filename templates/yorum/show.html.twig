{% extends 'frontbase.html.twig' %}

{% block title %}Yorum{% endblock %}

{% block body %}
    <h1>Yorum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ yorum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ yorum.name }}</td>
            </tr>
            <tr>
                <th>mesaj</th>
                <td>{{ yorum.yorum }}</td>
            </tr>
            <tr>
                <th>Productid</th>
                <td>{{ yorum.productid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('yorum_index') }}">back to list</a>

    <a href="{{ path('yorum_edit', {'id': yorum.id}) }}">edit</a>

    {{ include('yorum/_delete_form.html.twig') }}
{% endblock %}
